# 🚀 Быстрый деплой (5 минут)

## Вариант 1: Railway.app (Самый простой) ⭐

1. Зарегистрируйтесь на [railway.app](https://railway.app) (через GitHub)
2. Создайте новый проект → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Добавьте переменные окружения:
   - `API_ID` = ваш API ID
   - `API_HASH` = ваш API Hash
   - `CHECK_DISTRIBUTION_CHAT_ID` = -5011055445
   - `MAIN_BOT_TOKEN` = 8522550266:AAECDZt9GDn3sFOSAE6Uf8Mt4bgO4Rlaku8
5. Загрузите файл `accounts.txt` через Railway Dashboard → Variables → Files
6. Готово! Бот запустится автоматически

**Бесплатно:** $5 кредитов в месяц (достаточно для бота)

---

## Вариант 2: Render.com (Полностью бесплатно) ⭐⭐

1. Зарегистрируйтесь на [render.com](https://render.com) (через GitHub)
2. Создайте новый "Background Worker"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`
   - **Environment:** Python 3
5. Добавьте переменные окружения (Environment Variables)
6. Загрузите `accounts.txt` через SSH или создайте вручную
7. Готово!

**Бесплатно:** Полностью бесплатно (засыпает при бездействии, но для бота это не проблема)

---

## Вариант 3: PythonAnywhere (Для Python)

1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Перейдите в "Tasks" → "Always-on task"
3. Загрузите файлы проекта через "Files" tab
4. Создайте задачу:
   - **Command:** `cd /home/username/your-project && python3 main.py`
   - **Working directory:** `/home/username/your-project`
5. Создайте файл `.env` с переменными окружения
6. Загрузите `accounts.txt`
7. Запустите задачу

**Бесплатно:** Бесплатный план доступен

---

## ⚠️ Важно перед деплоем

1. **Создайте файл `accounts.txt`** с вашими аккаунтами (формат смотрите в `accounts.txt.example`)
2. **Убедитесь, что `Procfile` и `runtime.txt` созданы** (уже созданы)
3. **Проверьте `.gitignore`** - не загружайте секреты в Git!

---

## 📝 Что нужно для деплоя

- ✅ GitHub репозиторий с кодом
- ✅ API_ID и API_HASH (из https://my.telegram.org)
- ✅ Файл `accounts.txt` с аккаунтами
- ✅ Аккаунт на платформе хостинга

---

## 🆘 Проблемы?

**Бот не запускается:**
- Проверьте логи на платформе
- Убедитесь, что все переменные окружения добавлены
- Проверьте формат `accounts.txt`

**Сессии не сохраняются:**
- На бесплатных платформах сессии могут удаляться
- Рассмотрите сохранение в облачном хранилище

**Нужна помощь?**
- Смотрите подробное руководство в `DEPLOYMENT_GUIDE.md`


