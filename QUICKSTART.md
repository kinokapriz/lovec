# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Получение API credentials

1. Перейдите на https://my.telegram.org
2. Войдите в свой аккаунт
3. Перейдите в "API development tools"
4. Создайте приложение и получите `api_id` и `api_hash`

## Шаг 3: Создание файла аккаунтов

Создайте файл `accounts.txt` со следующим содержимым:

```
api_id:api_hash:session_name:phone
```

**Пример:**
```
12345678:abcdef1234567890abcdef1234567890:account1:+1234567890
12345678:abcdef1234567890abcdef1234567890:account2:+1234567891
```

**Примечание:** Если все аккаунты используют одинаковые API credentials, можете указать их один раз в `config.py` (строки 8-9), тогда формат будет просто:
```
session1:+1234567890
session2:+1234567891
```

## Шаг 4: Настройка конфигурации (опционально)

Откройте `config.py` и настройте:
- `MAX_CONCURRENT_CHECKS` - для увеличения скорости (рекомендуется 50-100)
- `CHECK_TIMEOUT` - таймаут активации (рекомендуется 2-3 секунды)
- `LOG_CHAT_ID` - ID чата для получения логов
- `ANTICAPTCHA_API_KEY` - ключ от 2captcha для решения капчи

## Шаг 5: Запуск

```bash
python main.py
```

При первом запуске для каждого аккаунта нужно:
1. Ввести код подтверждения из Telegram
2. Ввести пароль 2FA (если включен)

## 📊 Проверка работы

После запуска вы увидите:
- Количество подключенных аккаунтов
- Сообщения о найденных и активированных чеках
- Статистику работы

## ⚡ Оптимизация для максимальной скорости

1. **Увеличьте `MAX_CONCURRENT_CHECKS`** в `config.py` до 100-200 (если сервер мощный)
2. **Уменьшите `CHECK_TIMEOUT`** до 2 секунд
3. **Используйте VPS** с хорошим интернет-соединением
4. **Установите более мощный сервер** для 100+ аккаунтов

## 🐛 Решение проблем

### Аккаунт не подключается
- Проверьте правильность API_ID и API_HASH
- Убедитесь, что номер телефона указан с кодом страны (+7, +1, и т.д.)
- Проверьте интернет-соединение

### Чеки не ловятся
- Убедитесь, что боты (@CryptoBot, @xrocket_bot) в списке контактов
- Проверьте, что аккаунты не заблокированы ботами
- Посмотрите логи на наличие ошибок

### Низкая скорость
- Увеличьте `MAX_CONCURRENT_CHECKS` в config.py
- Используйте более мощный сервер
- Убедитесь, что интернет-соединение стабильное

## 💡 Советы

1. Начните с небольшого количества аккаунтов (5-10) для тестирования
2. Мониторьте логи на наличие ошибок
3. Используйте отдельные API credentials для каждого аккаунта (рекомендуется)
4. Регулярно проверяйте статистику в базе данных

## 📞 Поддержка

Если возникли проблемы, проверьте:
1. Правильность формата `accounts.txt`
2. Наличие всех зависимостей
3. Логи на наличие ошибок
4. Интернет-соединение


