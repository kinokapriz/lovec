# 🚀 Руководство по бесплатному размещению бота

## 📋 Бесплатные варианты хостинга (на месяц+)

### 1. **Railway.app** ⭐ (Рекомендуется)
**Бесплатно:** $5 кредитов в месяц (достаточно для небольшого бота)

**Преимущества:**
- Очень простое развертывание
- Автоматический деплой из GitHub
- Поддержка Python
- HTTPS из коробки

**Как развернуть:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Создайте новый проект
4. Выберите "Deploy from GitHub repo"
5. Railway автоматически определит Python проект
6. Добавьте переменные окружения:
   - `API_ID`
   - `API_HASH`
   - `ANTICAPTCHA_API_KEY` (если используется)
7. Запустите деплой

**Создайте файл `Procfile`:**
```
worker: python main.py
```

**Создайте файл `runtime.txt`:**
```
python-3.11.0
```

---

### 2. **Render.com** ⭐⭐
**Бесплатно:** Бесплатный план (засыпает через 15 минут бездействия, но для бота это не проблема)

**Преимущества:**
- Полностью бесплатно
- Автоматический деплой из GitHub
- Простая настройка

**Как развернуть:**
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`
   - **Environment:** Python 3
5. Добавьте переменные окружения в разделе "Environment"
6. Запустите деплой

**Важно:** Render засыпает при бездействии, но для бота это не критично - он будет продолжать работать.

---

### 3. **Fly.io**
**Бесплатно:** 3 shared-cpu-1x VMs, 3GB storage, 160GB outbound data transfer

**Преимущества:**
- Хорошая производительность
- Глобальная сеть
- Простая настройка

**Как развернуть:**
1. Установите Fly CLI: `curl -L https://fly.io/install.sh | sh`
2. Зарегистрируйтесь: `fly auth signup`
3. В корне проекта создайте `fly.toml`:

```toml
app = "your-bot-name"
primary_region = "fra"

[build]

[env]
  PYTHONUNBUFFERED = "1"

[[services]]
  internal_port = 8080
  protocol = "tcp"
  processes = ["app"]

  [[services.ports]]
    port = 80
```

4. Запустите: `fly launch`
5. Добавьте секреты: `fly secrets set API_ID=xxx API_HASH=xxx`

---

### 4. **Heroku** (ограниченно бесплатно)
**Бесплатно:** Heroku убрал бесплатный план, но можно использовать альтернативы

---

### 5. **PythonAnywhere** ⭐
**Бесплатно:** Бесплатный план (ограниченная функциональность)

**Преимущества:**
- Специально для Python
- Простая настройка
- Веб-интерфейс

**Как развернуть:**
1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Перейдите в "Tasks" → "Always-on task"
3. Загрузите файлы проекта
4. Настройте задачу:
   - **Command:** `python3 main.py`
   - **Working directory:** `/home/username/your-project`
5. Добавьте переменные окружения в файл `.env` или в настройках

**Ограничения бесплатного плана:**
- Задача должна работать постоянно (не засыпает)
- Ограниченные ресурсы CPU

---

### 6. **Replit** ⭐
**Бесплатно:** Бесплатный план с ограничениями

**Преимущества:**
- Встроенный редактор
- Простое развертывание
- Можно запускать из браузера

**Как развернуть:**
1. Зарегистрируйтесь на [replit.com](https://replit.com)
2. Создайте новый Python Repl
3. Загрузите файлы проекта
4. Установите зависимости: `pip install -r requirements.txt`
5. Добавьте переменные окружения в "Secrets"
6. Запустите: `python main.py`

**Ограничения:**
- Repl засыпает при бездействии (но можно использовать UptimeRobot для пинга)

---

### 7. **VPS (Vultr, DigitalOcean, AWS)** - С бесплатными кредитами
**Бесплатно:** Обычно дают $100-200 кредитов на первый месяц

**Vultr:**
- $100 кредитов на 60 дней
- Регистрация: [vultr.com](https://www.vultr.com)

**DigitalOcean:**
- $200 кредитов на 60 дней
- Регистрация: [digitalocean.com](https://www.digitalocean.com)

**AWS:**
- Free tier на 12 месяцев
- Регистрация: [aws.amazon.com](https://aws.amazon.com)

**Как развернуть на VPS:**
1. Создайте VPS (Ubuntu 22.04)
2. Подключитесь по SSH
3. Установите зависимости:
```bash
sudo apt update
sudo apt install python3 python3-pip git -y
```
4. Клонируйте проект:
```bash
git clone <your-repo> bot
cd bot
pip3 install -r requirements.txt
```
5. Создайте файл `.env`:
```bash
nano .env
# Добавьте переменные окружения
```
6. Запустите через `screen` или `tmux`:
```bash
screen -S bot
python3 main.py
# Нажмите Ctrl+A, затем D для отсоединения
```

**Для постоянной работы используйте systemd:**
Создайте `/etc/systemd/system/bot.service`:
```ini
[Unit]
Description=Telegram Check Bot
After=network.target

[Service]
Type=simple
User=your-username
WorkingDirectory=/home/your-username/bot
ExecStart=/usr/bin/python3 main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl enable bot
sudo systemctl start bot
```

---

## 🎯 Рекомендации по выбору

### Для быстрого старта:
1. **Railway.app** - самый простой вариант
2. **Render.com** - полностью бесплатно

### Для долгосрочного использования:
1. **VPS с бесплатными кредитами** (Vultr/DigitalOcean)
2. **Fly.io** - хороший баланс

### Для тестирования:
1. **PythonAnywhere** - просто и быстро
2. **Replit** - удобный редактор

---

## 📝 Подготовка к деплою

### 1. Создайте файл `.gitignore`:
```
sessions/
*.session
*.db
.env
__pycache__/
*.pyc
```

### 2. Создайте файл `requirements.txt` (уже есть в проекте)

### 3. Настройте переменные окружения:
- `API_ID`
- `API_HASH`
- `ANTICAPTCHA_API_KEY` (опционально)

### 4. Для Railway/Render создайте `Procfile`:
```
worker: python main.py
```

---

## ⚠️ Важные моменты

1. **Хранение сессий:** На бесплатных платформах сессии могут удаляться. Рассмотрите сохранение в облачном хранилище.

2. **База данных:** SQLite файл может быть удален. Используйте внешнюю БД (например, PostgreSQL на Render/Railway).

3. **Логи:** Настройте логирование в файл или внешний сервис.

4. **Мониторинг:** Используйте UptimeRobot для мониторинга работы бота (бесплатно).

---

## 🔧 Пример настройки для Railway

1. **Создайте `Procfile`:**
```
worker: python main.py
```

2. **Создайте `runtime.txt`:**
```
python-3.11.0
```

3. **Добавьте в Railway переменные окружения:**
- `API_ID`
- `API_HASH`
- `CHECK_DISTRIBUTION_CHAT_ID=-5011055445`
- `MAIN_BOT_TOKEN=8522550266:AAECDZt9GDn3sFOSAE6Uf8Mt4bgO4Rlaku8`

4. **Загрузите `accounts.txt` через Railway Dashboard** (Variables → Files)

---

## 🆘 Решение проблем

### Бот не запускается:
- Проверьте логи на платформе
- Убедитесь, что все зависимости установлены
- Проверьте переменные окружения

### Сессии не сохраняются:
- Используйте внешнее хранилище (S3, Google Drive API)
- Или синхронизируйте через git (не рекомендуется для безопасности)

### Бот засыпает:
- Используйте UptimeRobot для пинга (для Render/Replit)
- Или перейдите на VPS

---

## 📊 Сравнение платформ

| Платформа | Бесплатно | Засыпает | Простота | Рекомендация |
|-----------|-----------|----------|----------|--------------|
| Railway | $5/мес | Нет | ⭐⭐⭐⭐⭐ | Да |
| Render | Да | Да | ⭐⭐⭐⭐ | Да |
| Fly.io | Да | Нет | ⭐⭐⭐ | Да |
| PythonAnywhere | Да | Нет | ⭐⭐⭐⭐ | Для тестов |
| Replit | Да | Да | ⭐⭐⭐ | Для тестов |
| VPS (Vultr) | $100/60дн | Нет | ⭐⭐⭐ | Для долгосрочного |


